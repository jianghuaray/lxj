# 2026-05-06 工作记录

## 前端代码优化 — Task #22: global.scss !important 清理

### 完成内容
清理 `frontend/src/styles/global.scss` 中过度使用的 `!important`，从约 **90+ 处** 减少到 **20 处**。

### 清理策略
- **已移除 !important 的区域**（自定义类，不与第三方库冲突）：
  - 原生表单输入类：`.form-input`, `.filter-input`, `.filter-search`, `.input-field` 及其 placeholder/focus 状态
  - 原生 select 类：`.filter-select`, `.form-select`, `.pagination-info select`, `.settlement-header select`
  - `.form-textarea` 及其 focus 状态
  - `.filter-date`（width 属性）
  - 卡片类：`.form-card`, `.stat-card`, `.level-card`, `.tag-card`, `.table-wrapper` 等
  - 标题/标签：`.section-title`, `.card-title`, `.form-label`, `.required`
  - 数据表格：`.data-table thead th`, `tbody tr`, `tbody td`
  - 状态/徽章：所有 `.status-badge.*`, `.level-badge.*`, `.tag-*` 等颜色类
  - 分页按钮：`.page-btn` 及其 hover/active 状态
  - 空状态：`.empty-state`

- **保留 !important 的区域**（有合理原因）：
  1. **禁用状态**（3 处）：`.btn:disabled`, `.page-btn:disabled` — 防止交互覆盖
  2. **Element Plus el-select 组件覆盖**（17 处）：`.page-size-select .el-select__wrapper`, `.form-select-el`, `.pill-select-el` — EP 内部样式使用高优先级选择器，必须用 !important 才能覆盖

### 验证
- `npx vite build` 构建通过 ✓

### 三个优化任务完成状态
- Task #20: AbortController/cancel token ✅ (4个列表页已添加)
- Task #21: Dashboard ECharts 配置抽取 ✅ (chartConfig.js 已创建)
- Task #22: global.scss !important 清理 ✅ (90+ → 20)

## 删除确认弹窗统一改造

### 完成内容
将所有页面的 `ElMessageBox.confirm` 删除确认弹窗替换为自定义 `DeleteConfirmDialog` 组件，统一风格。

### 新建文件
- `frontend/src/components/DeleteConfirmDialog.vue` — 自定义删除弹窗组件（按 design-v2/pages/dialog-delete-v3.html 设计稿）
- `frontend/src/composables/useDeleteConfirm.js` — 命令式调用 composable，返回 `confirmDelete()` Promise

### 修改文件（6处替换）
1. **TechnicianList.vue** — 删除师傅（带 warning：工单变待派单）
2. **VolunteerList.vue** — 删除志愿者（带 warning：服务记录一并删除）
3. **VolunteerDetail.vue** — 删除志愿者（带 warning）、删除服务记录（无 warning）
4. **CustomerDetail.vue** — 删除客户（带 warning：工单记录保留）
5. **orders/OrderDetail.vue** — 删除工单（无 warning）

### 设计规格
- 圆角 `24px 16px 24px 16px`，overlay 毛玻璃 blur(4px)
- 红色圆形图标 56px，垃圾桶 SVG
- 药丸按钮：取消（边框）、确认删除（红色 destructive）
- 黄色警告框（可选）
- 入场动画 scale + translateY，ESC 关闭

## 师傅删除逻辑 + 客户详情删除按钮样式

### 修改内容
1. **后端 technicians.js 删除逻辑**：从"有派工记录就不能删"改为"只有已派单(dispatched)状态的工单才阻止删除"，completed 等状态允许删除
2. **TechnicianList.vue 删除弹窗文案**：warning 改为"如果师傅有已派单工单，则无法删除！"
3. **CustomerDetail.vue 删除按钮**：添加 `.btn-pill.danger-outline` 样式（红色边框胶囊按钮），匹配设计规范
