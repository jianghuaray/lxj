# 2026-05-03 工作日志

## 师傅管理 - 抽成比例字段名修复

**问题**：前后端字段名不匹配导致抽成数据无法正确保存和读取。

- 后端 Sequelize 模型使用 `underscored: true`，返回字段名为 `commission_rate`（蛇形）
- 前端代码读取的是 `commissionRate`（驼峰），导致一直 fallback 到默认值 30
- 修复文件：
  - `frontend/src/views/TechnicianAdd.vue`：读取 `data.commission_rate`，转成百分比显示
  - `frontend/src/views/TechnicianList.vue`：卡片展示读取 `tech.commission_rate`

## 工单管理详情页 - 施工信息卡片调整

**需求**：施工信息卡片字段排序改为「维修师傅 → 总费用 → 抽成比例」，抽成比例从对应师傅自动带出，不可修改。

**修改文件**：`frontend/src/views/orders/OrderDetail.vue`

### 展示态（cost-table）
- 抽成比例行从最后移到总费用行后面（第3位）

### 完成工单表单（dispatched 状态）
- 排序：维修师傅 → 总费用 → 抽成比例（只读）→ 应收服务费 → 已收服务费 → 材料成本
- 师傅选择加 `@change="onTechnicianChange"` 事件

### 编辑态（edit-form）
- 抽成比例 input 移到总费用后面，改为只读（disabled）
- 师傅选择加 `@change="onTechnicianChange"`

### JS 逻辑
- 新增 `formatCommissionRate(order)` 函数，兼容 `commissionRate` 和 `commission_rate` 两种字段名
- 新增 `onTechnicianChange(techId)` 函数：从 `availableTechnicians` 找到对应师傅，自动带出 `commission_rate` 填入表单，并重新计算 `serviceFee`
- 修改 `startEditConstruction()`：从 `availableTechnicians` 读取师傅的 `commission_rate`（在 fetchOrderDetail 之后调用，确保师傅列表已加载）
- 修改 `fetchOrderDetail()`：同样从师傅数据读取 `commission_rate`
- 移除 `commissionRate` 的 watch（不再手动编辑抽成比例）
- 保留 `totalFee` 的 watch，变化时用当前 `commissionRate` 重新计算 `serviceFee`

## 工单管理页面 KPI 卡片 Bug 修复

**问题**：
1. 点击卡片没反应
2. 数据统计不对

**原因分析**：
1. `debouncedSearch` 函数中 `pagination.page = 1` 缺少 `.value`，导致 ref 访问错误
2. 后端 `awaitCallbackCount` 查询只统计 `status='completed'`，但应该包括 `completed` 和 `callback` 两个状态

**修复文件**：
- `frontend/src/views/OrderList.vue`：第398行，`pagination.page = 1` → `pagination.value.page = 1`
- `backend/src/routes/workOrders.js`：第109-118行，`status: 'completed'` → `status: { [Op.in]: ['completed', 'callback'] }`

**验证**：前端 Vite build 和后端 Node.js 语法检查均通过

## 系统设置 - 基础数据管理完整 CRUD

**需求**：补齐系统管理页面的基础数据管理功能，包括服务类型、区域、来源渠道、取消原因的增删改查。

### 后端改动
**文件**：`backend/src/routes/settings.js`
- 重构为 `registerCRUD()` 通用注册函数，消除重复代码
- **新增 PUT 接口**（编辑/重命名）：`PUT /settings/:category/:name`
- 四个类别都支持完整 CRUD：GET(列表)、POST(新增)、PUT(编辑)、DELETE(删除)
- 编辑时校验原项存在、新名不重复

### 前端改动
**文件**：`frontend/src/views/SystemSettings.vue`
- **移除内联添加输入框**，改为统一的 `el-dialog` 弹窗方式（与设计图一致）
- **新增 `openAddDialog(listName)`** — 打开新增弹窗
- **新增 `openEditDialog(listName, item)`** — 打开编辑弹窗（预填当前值）
- **新增 `saveBaseDataItem()`** — 根据模式自动调用 POST 或 PUT API
- 新增弹窗相关状态变量和 categoryConfig 配置表
- 每个卡片增加空数据提示（"暂无数据，点击右上角添加"）
- el-dialog 统一样式（圆角、标题字体、分隔线）
