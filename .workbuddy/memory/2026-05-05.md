# 2026-05-05 工作记录

## 手机号验证移除 (VolunteerAdd)

### 修改内容
- **前端 VolunteerAdd.vue**：
  - 输入框改为只允许输入数字：`@input="form.phone = form.phone.replace(/[^0-9]/g, '')"`
  - 输入类型改为 `type="text"` + `inputmode="numeric"`（移动端更好用）
  - 删除正则验证 `/^1[3-9]\d{9}$/`，只保留必填校验
- **后端 volunteers.js**：
  - 删除 `const { validatePhone } = require('../utils/sanitize')` import
  - 删除 `validatePhone(phone)` 调用，不再校验手机号格式

### 效果
- 手机号输入框只能输入数字，不能输入字母或符号
- 不再强制要求 11 位手机号格式，只要填了数字即可提交

## 手机号验证移除（全系统）

### 后端修改（4个路由文件）
- `volunteers.js` - 删除 validatePhone import 和调用
- `customers.js` - 删除 validatePhone import 和调用，改为仅校验必填
- `workOrders.js` - 删除 validatePhone import 和调用
- `technicians.js` - 删除 validatePhone import 和调用

### 前端修改（api.js）
- 添加 2 秒去重机制，防止相同错误消息重复弹出（showError 函数）

## 客户新增/编辑页面重构

### 新增文件
- `frontend/src/views/CustomerAdd.vue` - 基于 design-v2/pages/customer-add-v3.html 设计稿
  - 两个卡片：基本信息 + 客户标签
  - 手机号输入限制只允许数字（computed phoneProxy）
  - 手机号 blur 时查询是否已存在客户（匹配提示）
  - 标签管理：可选标签 + 自定义标签
  - 支持新增和编辑模式（通过 route.params.id 判断）

### 路由修改
- `router/index.js` 添加 `/customers/add` 和 `/customers/edit/:id` 路由

### CustomerList.vue 修改
- "添加客户"按钮从 showDialog() 改为 $router.push('/customers/add')
- "编辑"按钮从 showDialog(customer) 改为 $router.push('/customers/edit/' + customer.id)
- 删除整个 el-dialog 模板（约35行）
- 删除 dialog 相关脚本：dialogVisible, editingCustomer, formRef, form, rules, showDialog, submitForm

## 师傅管理页面卡片对齐设计稿

### 修改内容（TechnicianList.vue）
- **卡片圆角**：从 6n 三交替改为 odd/even 两交替（匹配 technician-v3.html 设计稿）
- **卡片 header 对齐**：`align-items: flex-start` → `align-items: center`
- **卡片 header 间距**：`margin-bottom: 14px` → `16px`
- **底部按钮保持不变**（查看详情/编辑/结算）

### 设计稿差异来源
- `design-v2/pages/technician-v3.html` 的 `.tech-card-header` 使用 `align-items: center` 和 `margin-bottom: 16px`
- 卡片圆角设计稿是 nth-child(odd/even) 两交替模式

## 师傅管理页面按钮调整

### 修改内容（TechnicianList.vue）
- **删除"查看详情"按钮**：移除跳转到 `/technicians/${tech.id}` 的按钮
- **添加"删除"按钮**：使用 trash icon，调用已有的 `handleDelete(tech)` 函数（含确认弹窗）
- **按钮顺序调整**：结算 → 编辑 → 删除（参照截图）
- `handleDelete` 函数已存在（334-346行），无需额外修改

## 师傅删除逻辑修复

### 问题
- 后端删除接口（technicians.js 第388-402行）是**软删除**：只设 `status: 0`，记录仍在数据库
- 前端显示"删除成功"，但列表刷新后记录还在（只是状态变"停用"）

### 修复（backend/src/routes/technicians.js）
- `router.delete('/:id')` 中的 `await technician.update({ status: 0 })` 改为 `await technician.destroy()`
- 彻底删除记录，而非软删除

### 注意
- 如果师傅有关联的 Construction 记录，可能因外键约束删除失败
- 后端入口文件是 `src/app.js`（非 `src/index.js`）

## 师傅/志愿者管理页面对齐设计规范 V1.2

### 师傅管理页面（TechnicianList.vue）
- **去掉头像**：删除 `.tech-avatar` 和 `tech-header-left`/`tech-header-right` 容器，姓名和电话直接放在 header 左侧
- **去掉状态胶囊**：删除 `.status-pill`/`.status-dot`，状态改为卡片左侧 3px 彩色边条（绿 #4CAF50=启用，红 destructive=停用）
- **颜色对齐设计规范**：电话 #9A9A8E、统计数字 #5A5A50、统计标签 #9A9A8E、统计字重 700→600

### 师傅排序功能
- **前端**：状态筛选下拉框改为排序下拉框（本月单量高/低、满意度高/低、营收高/低）
- **后端**（technicians.js GET /）：
  - 新增 `sort` 参数支持（orderCount_desc/asc、avgSatisfaction_desc/asc、totalRevenue_desc/asc）
  - 新增 `totalRevenue` 字段（当月 Construction 表 total_fee 之和）
  - orderCount 改为当月统计（添加 created_at 月份过滤）
  - 排序采用全量查询+JS排序（因为排序字段是计算字段非DB字段）

### 志愿者管理页面（VolunteerList.vue）
- **颜色对齐设计规范**：电话 #9A9A8E、统计值 #5A5A50、统计标签 #9A9A8E、字重 700→600

## 全面代码审查

### 审查报告
- 生成完整审查报告：`代码审查报告-20260505-v2.md`
- 发现 **3 个 P0 严重问题**、**7 个 P1 重要问题**、**12 个 P2 中等问题**、**7 个 P3 低优先级问题**

### 关键发现
- **P0 Bug：** `frontend/src/utils/api.js` 第21行 `showError()` 无限递归调用自身，导致所有API错误场景浏览器崩溃（应改为 `ElMessage.error(msg)`）
- **P1 性能：** 师傅列表查询执行4-6次串行SQL查询 + 排序时全量加载再内存分页
- **P1 重复代码：** 派单(assign/dispatch)和回访(complete/:orderId)路由完全复制
- **P1 安全：** 志愿者搜索 LIKE 通配符未转义（volunteers.js 缺少 escapeLike）
- **P1 数据完整性：** FeeList.vue API失败时回退到硬编码模拟数据（getMockData），生产环境显示假数据
- **P2 数据不同步：** CustomerAdd.vue 区域/来源选项硬编码，未从系统设置API获取
- **P2 代码重复：** Dashboard.vue 四个图表渲染函数大量重复配置
- **P2 样式问题：** global.scss 大量!important；variables.scss和element-override.css重复定义变量
- **P2 工具缺失：** 无ESLint/Prettier配置
- **P3 XSS风险：** Token存储在localStorage
- **P2 缺失索引：** Volunteer/VolunteerService/CallbackRecord 表缺少必要索引

### 前端审查纠正
- 前端审查报告声称 `views/orders/` 目录不存在，经验证该目录实际存在且包含3个组件文件

## 批次一修复执行（11项全部完成）

### 修复的文件清单
1. **frontend/src/utils/api.js** — `showError(msg)` 递归 → `ElMessage.error(msg)` [P0]
2. **backend/src/routes/volunteers.js** — 添加 `escapeLike(keyword)` 包装搜索关键词 [P1]
3. **backend/src/routes/callbacks.js** — 删除 `POST /:orderId` 重复路由，保留 `POST /:id/complete` [P1]
4. **backend/src/routes/workOrders.js** — 提取 `handleAssign` 函数，dispatch 路由改为转发调用 [P1]
5. **backend/src/routes/customers.js** — DELETE 路由添加关联工单检查，返回友好错误 [P0]
6. **backend/src/routes/technicians.js** — DELETE 路由添加关联派工记录检查，返回友好错误 [P0]
7. **frontend/src/views/FeeList.vue** — 删除模拟数据回退（getMockData/calculateMockSummary），catch 改为空数组 [P1]
8. **frontend/src/views/CustomerAdd.vue** — 导入 settingsStore，硬编码选项改用 `settingsStore.areas` / `settingsStore.channels`，onMounted 添加 fetchAll [P2]
9. **backend/src/models/Volunteer.js** — community 字段添加 `index: true` [P2]
10. **backend/src/models/VolunteerService.js** — volunteerId 字段添加 `index: true` [P2]
11. **backend/src/models/CallbackRecord.js** — indexes 数组添加 `satisfaction_score` 索引 [P2]
12. **backend/src/app.js** — CORS fallback 改为生产环境不默认允许 localhost [P3]
13. **backend/package.json** — 删除无用依赖 cos-nodejs-sdk-v5 [P3]

### 注意事项
- CallbackRecord.js 已有 order_id/callback_by/callback_at 索引，仅补充 satisfaction_score
- 前端未使用 `/api/callbacks/` 路径，callbacks.js 整个文件可能未被前端调用（回访通过 workOrders.js POST /:id/callback 处理）
- workOrders.js 的 dispatch 路由现在先做字段名规范化（dispatchRemark→remark），再调用 handleAssign

## 批次一重启验证（2026-05-05 22:51）

### 验证结果
- ✅ **后端启动正常**（端口 3000）
- ✅ **前端启动正常**（端口 5173）
- ✅ **客户删除 FK 保护**：返回 `{"error":"无法删除：该客户有 2 条关联工单"}`，正确拦截
- ✅ **师傅删除 FK 保护**：返回 `{"error":"无法删除：该师傅有 1 条关联派工记录"}`，正确拦截
- ✅ **SQL 注入防护**：`%test%` 作为搜索关键词被 escapeLike 转义，返回空结果而非报错
- ✅ **Settings API**：正常返回服务类型列表
- ✅ **P0 修复验证**：api.js 的 `showError()` 内部已改为 `ElMessage.error(msg)`，不再递归
- ✅ **FeeList mock 移除**：文件中不再包含 getMockData/calculateMockSummary
- ✅ **CustomerAdd settingsStore**：4 处引用 settingsStore
- ✅ **回访重复路由已删除**：`POST /api/callbacks/1` 返回 404（Cannot POST），确认重复路由已移除
- ✅ **dispatch 路由正常**：返回 `{"error":"工单不存在"}`（999 是不存在的 ID），路由逻辑正确
